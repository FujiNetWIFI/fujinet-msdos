# fmall

fmall is a simple tool which mounts all of the available FujiNet slots; reporting success or failure. It's written in assembler, so it's small, and only requires the nasm (Netwide) assembler.

It also serves as a simple example of how to call FujiNet functions stationed at INT F5H.
